# OrderFare PO handoff — 2026-09-15 04:55 (addendum)

**Appends the 04:30 handoff. That document is still accurate except where this one
supersedes it — read it first, then this.** Same standing instructions: load the `orderfare`
skill, read `docs/PO-BRIEF.md`, re-run every check below rather than trusting these numbers.

The 04:30 file's §4 ("In flight right now — the lexicon fix") is **superseded**. Its §5, §6
and §7 still stand as written.

---

## 1. What changed in these 25 minutes

**The lexicon fix did not work, and Jason has ruled that item resolution moves into code.**

That is the whole of it. Everything below is consequence.

## 2. The finding — why the lexicon fix was rejected

The crew built it correctly and Vito's was already recompiled when I arrived (570 rows,
`provenance="derived"`, `created_at` 2026-09-15T08:18Z). Bare `cheeseburger` → `442f650d`
($8.49) is genuinely there. The 04:30 acceptance gate's first two steps passed.

The third step failed. I tabulated the crew's own 20-call matrix **by resolved item id**
rather than by schema validity:

| phrasing | resolved | |
|---|---|---|
| `cheeseburger` | 4/4 Cheese Burger $8.49 | ok |
| `2 cheeseburgers, medium` | 4/4 Bacon Cheeseburger **$10.99** | wrong |
| `two cheese burgers and a large fries` | 3/3 $8.49 | ok |
| `can i get a cheeseburger med and a coke` | 3/3 **$10.99** | wrong |
| `cheeseburger, fries, coke` | 3/3 $8.49 | ok |
| `lemme do a burger well done plus fries` | 3/3 **$10.99** | guess |

10 of 20 calls overcharge $2.50/unit. Then I ran a 30-call A/B of my own injecting the one
genuinely missing term, `cheeseburgers` → Cheese Burger:

| phrasing | A (deployed) | B (+`cheeseburgers`) |
|---|---|---|
| `2 cheeseburgers, medium` | 4/5 wrong | 4/5 wrong |
| `two cheeseburgers and a large fries` | 5/5 wrong | 5/5 wrong |
| `can i get a cheeseburger med and a coke` | 4/5 wrong | 1/5 wrong |

Adding the exact string the customer types changed nothing on the plural phrasings, and in
the failing sentence case the correct term was **already present in arm A**. Cheese Burger
is `orderable` at 849 — no menu-snapshot confound. This is not a data gap. The model ignores
the lexicon once the token sits in a sentence with other tokens.

Evidence, all on the Air: `~/po-scratch/finding-lexicon-fix-insufficient-20260915.md`,
`po-lexgap-ab-20260915.log` + `.py`, `propose-mx-v3-lexiconfix-20260915.log` (the crew's),
`lexvar-analysis-20260915.py`, `po-resolver-sim-20260915.py`, `po-suffix-measure-20260915.py`.

**Before speccing the fix I simulated it offline** (`po-resolver-sim-20260915.py`, no model,
no network to OpenRouter): deterministic longest-match over Vito's *live* lexicon gives
`cheeseburger` → $8.49, `bacon cheeseburger` → $10.99, `burger` → ambiguous across 7 items.
The dearer row can only win when the customer actually says "bacon". Do this before you spec
anything here — it cost two minutes and it de-risked the whole dispatch.

## 3. Jason's two rulings

1. **"approve moving resolution into code"** (~04:50).
2. **"I want it to ask for clarification. People hate when AI assumes and would much rather
   answer a reasonable clarifying question."** (~04:58) — this is the more durable one. It is
   now a standing constraint in `PO-BRIEF.md` §7, generalised past this resolver to every
   ambiguity the bot meets: item identity, size, option, new-line-vs-modify. **Never** add a
   tiebreak, popularity or relevance score, cheapest-wins rule, "most likely" heuristic, or a
   fall back to the model to suppress a clarifying question. A guess that lands on a cheap
   row is still a guess. Asking is the product behaviour, not a cost to engineer away later.

## 4. The spec

`docs/specs/2026-09-15-code-owned-resolution.md` — commits `2c3c05f9` then `1b3f21b9`.
Supersedes turn-engine §3c (the proposal contract) and the Phase 2 gate. Everything else in
`2026-09-14-turn-engine-oversight.md` stands, **including the Phase 0 freeze on `index.ts`**.

Shape of it:

- `adds[].menu_item_id` → `adds[].item_span`, the verbatim run of the customer's own message.
  `quantity` stays an integer field; `choices` stay id pairs; `removes`/`modifies` stay
  `line_key`; `answer_text` stays question-only.
- **This is not `source_phrase` returning.** That fed the text-grounding reconciler, which
  re-derived after the fact what had already happened, from prose. A span is an input to a
  deterministic total function that runs *before* anything reaches the cart, and nothing
  downstream ever reads it again. The spec says this explicitly — the crew will otherwise
  either refuse the contract change or rebuild the reconciler. Watch for both.
- `resolve-item.ts`, new pure module: normalise, longest lexicon term occurring in the span as
  a whole-word run, return exactly `resolved` / `ambiguous` / `unresolved`. DECIDE calls it;
  ambiguous and unresolved both route to ASK.

## 5. Dispatched and in flight — work item 1

`00-M-compile-menu-data-pass`, delivered 04:48:32, crew confirmed building.

Commit the (still uncommitted) variant pass, then add two more derived surface forms under
the same single uniqueness gate. **I measured both across all three real shops before
speccing them** — do not let anyone re-derive these numbers, and do not let anyone ship an
unmeasured third rule:

- **plural-of-collapsed** — the pass pluralizes only *stated* terms today, so `cheese burger`
  yields `cheese burgers` and `cheeseburger` but never `cheeseburgers`, the likeliest thing a
  customer types. Zio's +466, NJB +182, Vito's +0, **zero** new collisions anywhere. Five of
  Zio's 466 are currently swallowed by another item's term (`cheesesteaks`,
  `chickencheesesteaks`, `eggplantparmigianas`, `grilledchickens`, `shrimpparmigianas`) —
  the exact shape of the cheeseburger bug, sitting there live.
- **trailing word-runs** — `10 pieces wings boneless` → `wings boneless`, `boneless`. Kept /
  dropped-ambiguous: Vito's 158/193, Zio's 350/199, NJB 177/63.

**Its gate is a pure data assertion — no model calls, no matrix.** Unit tests; recompile
**Vito's only**; query `lexicon` and assert `cheeseburger`→`442f650d`,
`cheeseburgers`→`442f650d`, `bacon cheeseburger`→`94557b9c`, `bacon cheeseburgers`→`94557b9c`,
and `fries` / `burger` **ABSENT**; then diff the whole term→target map before/after and assert
**additions only** — if any existing term moved target or vanished, stop, do not "fix" it.

**Run that acceptance yourself.** The 04:30 handoff's rule applies with force here: the last
gate passed on the crew's report while billing $2.50 too much.

## 6. Open — work item 2, needs your go-ahead

`resolve-item.ts` + the `item_span` contract change, spec §4. **Not dispatched.** Blocked on
item 1 passing. Its gate is in the spec: unit tests whose fixtures can actually fail (at
least one item with two or more option groups — Cheese Burger has one and hid a defect once
already), then a live matrix at **five runs per phrasing** asserting resolved item id **and
price**, with an expected-outcome table that includes phrasings which must ASK. Zero calls may
bill Bacon Cheeseburger for a span not containing `bacon`.

Then Phase 3 (wire it into `index.ts`, one routing branch, `dialogue_state` migration,
per-shop `turn_engine_enabled`, Vito's on) — unchanged from the 04:30 handoff, still needs its
own dispatch, still review that diff personally.

## 7. A thing you should know about `fries`

`fries` matches **10** Vito's items and `burger` matches **7**, so under the resolver both
become a clarifying question where today the model silently picks one. I put this to Jason as
a possible regression and he confirmed it is what he wants. So when the matrix shows
`cheeseburger, fries, coke` asking "which fries?" where it used to resolve silently — **that
is a pass, not a regression.** Do not let anyone "fix" it.

## 8. Verified state at 04:55

| Fact | Value |
|---|---|
| HEAD | `1b3f21b9` (docs); last code commit `f686df46` |
| Deployed `chat-sms` | `3a5723a9` — **unchanged**, nothing deployed this session |
| Uncommitted | `compile-menu.ts` + `.test.ts` (crew is mid-build on these) |
| Courier | healthy, queue 0, last TICK 04:54:21 |
| Crew | building work item 1 |
| Vito's lexicon | menu `54a42842`, 1349 active rows, 570 `derived` @ 08:18Z |
| Canary | last verified 20/20 by the prior thread; **not re-run this session** |

`QUEUE.md` rewritten twice — the stale "Phase 2 blocked on a data ruling" item is gone, work
item 1 is listed as open and dispatched, item 2 as blocked, and the idle-kick tail now names
`00-M` instead of saying nothing is open. Keep it current or the 5-minute cron hands the crew
wrong work.

A watch is running at **:13 and :36** past the hour (session-only; it dies with that thread —
**set your own**). It checks courier health, reads the `00-M` reply, runs item 1's acceptance
independently, dispatches item 2 as `00-N-resolve-item` if it passes, and stays silent unless
money, a Vito's live-path regression, or a decision only Jason can make.

## 9. First moves

1. Re-run §8's checks. Do not trust this file's numbers.
2. `cat ~/.openclaw-sprintai/po-inbox/00-M-compile-menu-data-pass.reply` and check whether
   `compile-menu.ts` is committed.
3. When it lands, run item 1's acceptance **yourself** — the six lexicon assertions and the
   additions-only diff. It needs no model calls.
4. On a pass: dispatch work item 2 from spec §4, update `QUEUE.md`, and tell Jason it is
   moving. On a fail: do **not** let the crew touch Zio's or NJB, and tell Jason.
5. Set your own watch, 20–30 minutes.

Jason is directing from the road. Two to four lines, answer first. Notify only for money
wrong, a live-path regression on Vito's, a decision only he can make, or something he asked
for being ready.
