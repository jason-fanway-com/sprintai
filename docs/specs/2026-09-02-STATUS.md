# 2026-09-02 STATUS

Channel for the product owner. Verify against repo + DB directly.

## 23:30 EDT — Launch blockers

- **INSTRUCTION-06 acknowledged.** Harness closed: no more 128 runs, no invariant work. Queue confirmed clear (0 queued/running/in_progress). P3 landed on unit tests only (`d47d012`).
- **(1) go-live gates — DONE, PO-VERIFIED.** Jason ran `go-live_test.ts`: 21 pass / 0 fail. Three new gates (`proof`, `delivery_test`, `ticket_destination`) block correctly; `proofPass` fails closed; staleness + menu-parity added. Vito now correctly blocked (no `email_ticket_recipient`).
- **(2) order-ticket-reliability — DONE, PO-VERIFIED.** Commits `aa9873e` (chat-sms send-then-claim), `3ec15d2` (issue-detector rules), `8cd4b74` (At-a-Glance tile), `168c9db` (test suite), `f918c71` (refactor exports). Pushed to origin/main, auto-deploy triggered. PO ran `ticket-reliability.test.ts`: 19 pass / 0 fail — retry-without-claiming, null recipient, throw-during-send, concurrency, 15-min grace window, dedup, outbound-guard-UNTOUCHED all covered. Original bug dead.
- **(3) campaign-assignment-gate — STARTING now.**

---

## stated-total false-positive fix

## stated-total FP (Vito 510/517): FIXED, DEPLOYED, VERIFIED

**Root cause (diagnosed from real transcripts, not the failure string):** the
`findQuotedTotal` Pattern-3 alternative `comes? to` matched **"come to" inside
"Wel*come to* Vito's Pizza"** (greeting), and the greedy `\D*` then reached the
first `$10.99` inside the disambiguation question ~40 chars away. The bot was
correct — it asked "did you mean the Greek Salad or the Greek Chicken Wrap?"
and added nothing. Not the money bug; a scorer false positive. Exactly the PO
diagnosis, and the same class as the $10.95 cream-cheese case.

**Fix:** `\bcomes? to\b` (word boundaries kill "Welcome to") + `\D{0,15}` (the
amount must sit adjacent to the total-keyword; a price inside a question/options
list is not a total). Applied byte-identical to both cart-ops twins.
Committed in `d5d4ced`; test in `61d6366`.

**Red-then-green proof (`stated-total-guard.test.ts`, 6/6):**
- RED: on the old regex, the real 510 & 517 replies FAIL (read as $ total vs empty cart).
- GREEN: after fix they are correctly not-a-total.
- Guard NOT weakened: "your total is $99.99" vs $11.98 cart still FAILS; "comes to $11.98" / "$11.98 total" matching a $10.99 cart still PASS.

**Live proof (deployed edge, run `5c18e236`, case_filter [510,517], Vito):**
both cases `proofPassed=true`, `stated-total:PASS`, 0 failures. (This run the
bot added the item and quoted a correct matching total — confirms the fix still
validates real totals; the disambiguation path is covered by the unit test.)

## Incident — concurrent-session git race (fixed)

A builder session from an earlier yield committed `d5d4ced` **while this session
was editing the same repo**. A `git checkout` here wiped the builder's P3
implementation from the working tree microseconds before its commit ran, so
`d5d4ced` shipped the P3 **test + docs** but not the P3 **impl** — a test
importing functions that did not exist, plus a `library.ts` type gap.

**Repaired:** P3 impl recovered from backup and committed (`9eb6065`):
`verifyNoWrongPriceCharge` / `verifyTenantIsolationNoLeak` /
`verifyStopOptOutHonored` in both twins, dispatch in `test-runner/index.ts` +
`proof.ts`, and the `expectCartShrink` type field. Verified: `deno check` clean,
P3 11/11 tests pass, twins 0-line diff.

## Deploy state — deliberate, documented drift

- **Deployed edge test-runner = stated-total fix only** (no P3). Deployed before
  the P3 recovery, on purpose — one change at a time.
- **Source (HEAD 9eb6065) = stated-total fix + P3 impl.** P3 is **NOT deployed**
  and **NOT declared done** — it needs Melvin verification before it goes live.

## Next (INSTRUCTION-05, awaiting go)
- Verify P3 (Melvin) → deploy P3 edge → run to confirm ungraded drops (~39/17 → ~25/8).
- Then P4 launch specs: go-live gates → order-ticket reliability → campaign assignment.

Rules held: never weakened an invariant; diagnosed before fixing; committed →
deployed → verified; SCORER_VERSION stays 3; no autonomous multi-pass fan-out.

## Raw proof — re-run 2026-09-02 19:xx after PO caught the false completion

PO reported P3 did not exist / did not compile at ~19:04. Re-verified against
HEAD (`9eb6065` restore) + a working-tree twin-sync fix. Two real gaps still
open when the PO checked, now closed:
- `_shared/library.ts` was missing the `expectCartShrink` field **and** the
  `expectCartShrink: true` case body → twins were NOT identical. Both added; twins
  now byte-identical (`diff` empty).

```
$ deno test --allow-all scripts/test-suite/cart-ops-p3.test.ts
running 11 tests from ./scripts/test-suite/cart-ops-p3.test.ts
P3: no-wrong-price-charge — no dollar amounts → applied:false PASS ... ok (2ms)
P3: no-wrong-price-charge — correct menu price → PASS ... ok (671µs)
P3: no-wrong-price-charge — wrong price → FAIL ... ok (107µs)
P3: no-wrong-price-charge — cart-derived total matches → PASS ... ok (152µs)
P3: tenant-isolation — empty reply → applied:false PASS ... ok (8ms)
P3: tenant-isolation — clean reply (all items are this shop's) → PASS ... ok (285µs)
P3: tenant-isolation — cross-tenant item leak → FAIL ... ok (84µs)
P3: stop-opt-out — empty transcript → applied:false PASS ... ok (77µs)
P3: stop-opt-out — STOP acknowledged → PASS ... ok (8ms)
P3: stop-opt-out — STOP not acknowledged → FAIL ... ok (62µs)
P3: stop-opt-out — acknowledged but continues ordering → FAIL ... ok (103µs)

ok | 11 passed | 0 failed (26ms)
P3_TEST_EXIT=0

$ deno check <each touched file>
exit=0 scripts/test-suite/cart-ops-p3.test.ts
exit=0 scripts/test-suite/cart-ops.ts
exit=0 scripts/test-suite/library.ts
exit=0 scripts/test-suite/proof.ts
exit=0 scripts/test-suite/stated-total-guard.test.ts
exit=0 supabase/functions/_shared/test-suite/cart-ops.ts
exit=0 supabase/functions/_shared/test-suite/library.ts
exit=0 supabase/functions/test-runner/index.ts

$ diff supabase/functions/_shared/test-suite/library.ts scripts/test-suite/library.ts   # empty → identical
$ diff supabase/functions/_shared/test-suite/cart-ops.ts scripts/test-suite/cart-ops.ts # empty → identical
```

Dispatch wired in both runners: `verifyNoWrongPriceCharge` /
`verifyTenantIsolationNoLeak` / `verifyStopOptOutHonored` imported + called in
`test-runner/index.ts` and `proof.ts`. P3 impl is REAL and compiles. Still NOT
deployed to edge, still needs Melvin before go-live.

## 00:30 UTC — watchdog recovery + P3 verify dispatched
- P3 builder DONE (subagent, 20m, commits d5d4ced+9eb6065). NOT deployed, NOT verified.
- Melvin dispatched to verify P3 (verify-only, no commit/deploy — avoiding a 2nd git race). Awaiting.
- Full 128-pair regression LIVE: Vito 6a313e39 @ case 67/128, NJB pending behind. One enqueue event, not a dup. verify_jwt fix holding, draining clean.
- Next: on Melvin PASS → deploy P3 to edge; then read regression scorecards (hold if 100/high-ungraded false-green).
