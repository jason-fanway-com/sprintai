# STATUS — 2026-09-04

**Governing spec:** INSTRUCTION-10 (supersedes 07/08/09 sequencing).
**Mode:** build all components A–N independently; validate each on its own; Jason runs the
integration test. Rehearsal stood down.

**Now:** Item C — **built + deployed (onboarding-save v48, migration 084 live)**. Moving to next item.

**Board:** `docs/specs/2026-09-03-READINESS.md` (one row per A–N).

## Cursor
- Rehearsal: STOOD DOWN. Leg 1 cancelled, leg 2 not started, `p2-rehearsal-diner` retained.
- Item A: BUILT + fixes applied. Proofs running (NJB clone 128 cases, Vito QA 128 cases).
- Everything else: not started.

## Item A log (2026-09-04)
- `0e9cadf` — create-subscription edge fn + setup.html subscription step (John Walsh build)
- `486cd15` — onboarding-save ALLOWED_FIELDS: removed subscription_status/pm_set/sub_id (Melvin catch)
- `6e95bce` — stripe-webhook: stripe_customer_id → stripe_platform_customer_id (column rename fix)
- `213794c` — wizard.js: remove client subscription_status write (AC#1)
- Env var STRIPE_PRICE_SUBSCRIPTION: set in Supabase (price_1UBs7wFPm1l8Fm1Tl4w7QHaL, $99/mo)
- Coupon: FOUNDING-BETA-6MO-SUB (100% off, 6 months) · promo code FOUNDING (max 15, expires 2027-01-01)
- stripe-webhook redeployed (version 54)
- Proofs: running — results pending
